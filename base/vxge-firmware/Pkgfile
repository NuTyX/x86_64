description="vxge - Exar X3100 Series 10GbE PCIe I/O Virtualized Server Adapter"
url='https://git.kernel.org/?p=linux/kernel/git/firmware/linux-firmware.git;a=summary'

packager="tnut <tnut@nutyx.org>"
contributors="Spiky"

license="Custom"

set=(linux-firmware)

name=vxge-firmware
version=20241017

makedepends=(rdfind)
source=(WHENCE
        https://git.kernel.org/pub/scm/linux/kernel/git/firmware/linux-firmware.git/snapshot/linux-firmware-$version.tar.gz)

build() {

cd linux-firmware-$version

# delete this python check as long we don't know what's missing here
sed -i /check_whence.py/d copy-firmware.sh

cp ../WHENCE .
make DESTDIR=$PKG FIRMWAREDIR=/lib/firmware install

install -Dt $PKG/usr/share/licenses/$name -m644 WHENCE
}
