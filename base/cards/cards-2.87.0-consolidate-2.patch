diff --git a/scripts/pkgmk.in b/scripts/pkgmk.in
index bfdf30b..34815cb 100755
--- a/scripts/pkgmk.in
+++ b/scripts/pkgmk.in
@@ -751,7 +751,9 @@ add_meta_to_archive() {
 		echo "P$PACK" >> .META
 		echo "L$LICENSE" >> .META
 		echo "B$PKGMK_BUILDVER" >> .META
-    fi
+	else
+		echo "g$GROUP" >> .META
+	fi
 
 	# TODO improve the pkginfo --runtimedepfiles functionnality
 	if [ "$PKGMK_IGNORE_RUNTIMEDEPS" == "no" ];then
@@ -776,7 +778,6 @@ add_meta_to_archive() {
 			done
 		fi
 		if [ ! -z "$GROUP" ]; then
-			echo "g$GROUP" >> .META
 			rds=("run_$GROUP[@]")
 			if [ ! -z "$(echo ${!rds})" ]; then
 				info "Adding runtime deps to Archive $NAME"
