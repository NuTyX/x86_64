description="CAC (Common Access Cards) library that provides emulation of smart cards to a virtual card reader running in a guest virtual machine"
url="https://gitlab.freedesktop.org/spice/libcacard"

license="LGPL-2.1-or-later"

packager="tnut <tnut@nutyx.org>"
contributors="Rems,Tnut"

makedepends=(glib nss)

name=libcacard
version=2.8.1
release=2

source=(https://www.spice-space.org/download/$name/$name-$version.tar.xz)

package() {
# Obsolet libs
rm $PKG/usr/lib/libcacard.la

}
