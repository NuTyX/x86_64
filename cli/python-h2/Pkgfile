description='HTTP/2 State-Machine based protocol implementation'
url='https://pypi.org/project/h2/'

packager="spiky <spiky@nutyx.org>"
contributors="Tnut"

makedepends=(python-wheel python-installer python-setuptools python-hyperframe python-hpack)

name=python-h2
_name="h2"
alias=(${_name})

version=4.4.0

source=(https://github.com/python-hyper/hyper-h2/archive/v$version/$name-$version.tar.gz)

build() {

  cd ${_name}-$version

  python -m build --wheel --no-isolation
  python -m installer --destdir=$PKG dist/*.whl

  install -Dm644 LICENSE -t $PKG/usr/share/licenses/$name
}
