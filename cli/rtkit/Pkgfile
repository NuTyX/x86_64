description="Realtime Policy and Watchdog Daemon"
url="https://github.com/heftig/rtkit"

packager="tnut <tnut@nutyx.org>"
contributors="Spiky"

name=rtkit
version=0.14
release=2

makedepends=(systemd meson dbus polkit vim)

source=(https://gitlab.freedesktop.org/pipewire/rtkit/-/archive/v${version}/rtkit-v${version}.tar.gz)
PKGMK_GROUPS=()

prepare() {
  mv ${name}-v${version} ${name}-${version}
}

build=meson
