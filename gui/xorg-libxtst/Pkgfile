description="library for XTEST & RECORD extensions"
url="https://gitlab.freedesktop.org/xorg/liblibxtst"

packager="tnut <tnut@nutyx.org>"
contributors="Pierre,Greg,Spiky"

name=xorg-libxtst
alias=(libXtst)
version=1.2.5
release=1

_name=libXtst
alias=(libXtst libxtst)

makedepends=(xorgproto xorg-libx11 xorg-libxext xorg-libxi)
source=(https://xorg.freedesktop.org/releases/individual/lib/${_name}-$version.tar.xz)

build=xorg

package() {
rm $PKG/usr/lib/${_name}.la
}

uptodate() {
local ext url name LINX
LYNX="lynx -read_timeout=20 -dump -listonly -nonumbers "
ext=".tar.bz2"
url="https://ftp.x.org/archive//individual//lib"
name="libXtst-"
$LYNX $url/?C=M\;O=D|grep "$ext$"|grep "$url/$name"|sed "s@$ext\$@@"|sed "s@$url/$name@@"|head -1
}
