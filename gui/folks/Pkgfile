description="Library to aggregates people into metacontacts"
url="http://www.gnome.org/"

license="GPL2-or-later"

packager="spiky <spiky@nutyx.org>"
contributors="Fanch,Jayce,Tnut"

makedepends=(intltool libgee telepathy-glib python-dbusmock
             evolution-data-server gobject-introspection vala)

name=folks
version=0.15.12
release=1

source=(https://download.gnome.org/sources/$name/${version%.*}/$name-$version.tar.xz)

build=meson

package() {
 install -Dm644 $name-$version/COPYING \
 $PKG/usr/share/licenses/$name/COPYING
}
