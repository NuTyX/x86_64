description='Tool and library for running OCI-based containers in pods'
url='https://github.com/containers/libpod'

packager="spiky <spiky@nutyx.org>"
contributors=""

makedepends=(go-md2man libseccomp gpgme)
run=()
set=()

name=podman
version=5.3.1

source=(https://github.com/containers/podman/archive/v$version/$name-$version.tar.gz)

build() {
cd $name-$version

	export PATH=/usr/lib/go/bin:$PATH
	export GOFLAGS="-buildmode=pie"
	make  BUILDTAGS="seccomp exclude_graphdriver_devicemapper exclude_graphdriver_btrfs"
	make install PREFIX=/usr DESTDIR=$PKG LIBEXECDIR=/usr/lib

	# not use in venom: usr/lib/systemd
	rm -rf $PKG/usr/lib/systemd

}
