description='A backend implementation for xdg-desktop-portal for the GNOME desktop environment'
url='https://download.gnome.org/'

packager="spiky <spiky@nutyx.org>"
contributors="Tnut"

makedepends=(gnome-desktop gtk4 libadwaita nautilus wayland)
run=(xdg-desktop-portal-gtk)

set=(gnome)

name=xdg-desktop-portal-gnome
version=50.0
release=2

source=(https://download.gnome.org/sources/$name/${version%.*}/$name-$version.tar.xz)

PKGMK_GROUPS=()

build=meson

package() {
 install -Dm644 $name-$version/COPYING \
 $PKG/usr/share/licenses/$name/COPYING
}
