description='Video player for GNOME'
url='https://apps.gnome.org/Showtime/'

packager="spiky <spiky@nutyx.org>"
contributors="Tnut"

makedepends=(git blueprint-compiler gstreamer-plugins-base)
run=(gstreamer gstreamer-plugins-base gstreamer-plugins-good
     libadwaita python-gobject gtk4)
set=(gnome)

name=showtime
version=50.0
release=3

source=(https://gitlab.gnome.org/GNOME/showtime.git)

build=meson

package() {

python -m compileall -d /usr/lib $PKG/usr/lib
python -O -m compileall -d /usr/lib $PKG/usr/lib

}
